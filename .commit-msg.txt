docs: reflejar Railway como hosting actual (no Vercel)

La plataforma vive 100% en Railway (app + Postgres en el mismo proyecto)
desde la migracion Neon -> Railway en mayo 2026. Varios docs y comentarios
todavia mencionaban Vercel/Neon como host operativo.

Cambios (solo doc/comentarios, sin tocar logica):
- CLAUDE.md: hosting, build pipeline y "Informacion que NO debes pedir"
  ahora apuntan a Railway.
- docs/PROJECT_CONTEXT.md: diagrama de arquitectura, env vars de produccion
  y nota de migracion final apuntan a Railway.
- docs/PROJECT_STATUS.md: resumen ejecutivo, fila de tabla "Deploy" y
  notas de despliegue actualizadas a Railway.
- .env.example: DATABASE_URL ejemplo apunta a Railway proxy, NEXTAUTH_URL
  ejemplo usa el dominio Clariva.
- app/digital-dent-super-admin/layout.tsx + error.tsx: mensajes al usuario
  y comentario de logs mencionan Railway.
- prisma/seed-super-admin.ts + seed-multi-tenant.ts: comentarios de
  cabecera mencionan Railway en lugar de Vercel.

NOTA: docs/AI_CHANGELOG.md y docs/SESSION_HANDOFF.md NO se tocan porque son
registros historicos del momento de la migracion; reescribirlos seria
rescribir la historia del proyecto.
